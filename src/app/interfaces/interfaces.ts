export interface ListSandboxes {
  id: number;
  name: string;
}

export interface Employee {
  id: number;
  firstName: string;
  lastName: string;
  tech?: string;
}

export interface englishLevelList {
  id: number;
  name: string;
}
